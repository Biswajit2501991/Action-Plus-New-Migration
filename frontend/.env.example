# Proxied to Express backend (rewrites /api/* except Next route handlers)
API_PROXY_TARGET=http://127.0.0.1:4000
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_AUTH_COOKIE_MODE=false

# WhatsApp Verification: Next proxies to Express (API_PROXY_TARGET) first.
# Fallback local handler only needs these if the Express route is missing:
SUPABASE_URL=https://oddggoiyckyxolmqqjrf.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
APG_GYM_ID=48815df4-6144-40dd-bbd6-91fd8522d1ff

# Next.js V2 UI port (unique — do not use 3000)
PORT=3055
