# Secrets — never commit
.env
.env.local
.env.prod
backend/.env

# Local SQLite database and backups
data/
backend/data/
*.db
*.db-shm
*.db-wal

# Runtime logs
logs/
*.log

# Dependencies
node_modules/
backend/node_modules/
v2/node_modules/

# V2 frontend build (generated by npm run build:v2)
v2/dist/
v2/tsconfig.tsbuildinfo

# Next.js experiment (not used in production tunnel)
web/

# Cloudflared binary (install via brew)
tools/cloudflared/cloudflared
tools/cloudflared/cloudflared.tgz

# Build / test cache
node_modules/.vite/
.vitest/
coverage/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
