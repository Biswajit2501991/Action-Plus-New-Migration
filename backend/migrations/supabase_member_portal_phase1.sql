-- Member Portal Phase 1 — applied via MCP to shared Supabase.
-- Columns on members: member_uuid, portal_enabled, portal_status, qr_token, pin_hash,
--   portal_activated_at, last_portal_login_at
-- Tables: member_portal_otp_challenges, member_portal_devices,
--   member_portal_sessions, member_portal_audit_logs

SELECT 1;
