# Prefer the repo-root railway.toml for Railway.
# Root Directory must be "/" (repository root) because backend imports ../../src/*.
[build]
builder = "RAILPACK"
buildCommand = "npm ci"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
