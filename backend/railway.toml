# Railway: set service Root Directory to "backend".
[build]
builder = "RAILPACK"
buildCommand = "npm ci"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
