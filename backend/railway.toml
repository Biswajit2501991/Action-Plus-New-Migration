# Prefer the repo-root railway.toml for the API Railway service.
# Root Directory must be "/" because backend imports ../../src/*.
[build]
builder = "RAILPACK"
buildCommand = "npm ci"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
